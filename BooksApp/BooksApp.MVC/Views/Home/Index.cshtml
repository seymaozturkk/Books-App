@{
    Layout = "_Layout_Index";
}
@model List<BookModel>
@{
    ViewData["Title"] = "Ana Sayfa";
    List<BookModel> books = Model;
}
<!-- Slider -->
@await Html.PartialAsync("_SliderPartial")
<!-- Products -->
@await Html.PartialAsync("_BooksPartial", books)
<!-- Services -->
@await Html.PartialAsync("_ServicesPartial")

















@*@model List<BookModel>
@{
    ViewData["Title"] = "Ana Sayfa";
    List<BookModel> books = Model;
}
<div class="row">
    <div class="col-md-2 col-sm-2 col-12">
        <h1 class="display-6 fs-4 text-danger fw-normal ps-1">Kategoriler</h1>
        @await Component.InvokeAsync("Categories")
    </div>
    <div class="col-md-10 col-sm-10 col-12">
        <h1 class="display-6 fs-4 text-danger fw-normal ps-1">
            @(ViewBag.SelectedCategoryName==null ? "Tüm Kategoriler" : "Kategori: " + ViewBag.SelectedCategoryName)
        </h1>
        @if (Model.Count() > 0)
        {
            <div class="row g-3">
                @foreach (var book in books)
                {
                    <div class="col-md-3 col-sm-6 col-12">
                        @await Html.PartialAsync("_BookPartial", book)
                    </div>
                }
            </div>
            <div class="mt-2">
                <p>Toplam <span class="text-danger">@books.Count</span> adet kitap listelenmiştir...</p>
            </div>
        }
        else
        {
            @await Html.PartialAsync("_NoRecord","Kitap")
        }

    </div>
</div>
*@